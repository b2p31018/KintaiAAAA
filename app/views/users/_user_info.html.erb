<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <td>
        <%= link_to "⇦ 前月へ", user_path(@user, date: @first_day.prev_month), class: "btn btn-primary" %>
        【<%= l(@first_day, format: :middle) %>】勤怠管理表
        <%= link_to "次月へ ⇨", user_path(@user, date: @first_day.next_month), class: "btn btn-primary" %>
      </td>
      <td>
        指定勤務開始時間：9:00<br/>
        指定勤務終了時間：18:00
      </td>

      <td colspan="3" style="text-align: center; vertical-align: middle;">
          基本時間：<%= format_basic_info(@user.basic_time) %>
      </td>
      <td style="text-align: center; vertical-align: middle;">
        初日：<%= l(@first_day, format: :short) %>
      </td>
    </tr>

    <tr>
      <td>所属：<%= @user.department.present? ? @user.department : "未所属" %></td>
      <td>氏名：<%= @user.name %></td>
      <td>コード</td>
      <td><%= @user.id %></td>
      <td>出勤日数：<%= @worked_sum %>日</td>
      <td>締め：<%= l(@last_day, format: :short) %></td>
    </tr>
  </table>
</div>